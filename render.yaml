services:
  - type: web
    name: atcoder-tagged-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install && mkdir -p data && base64 -d /etc/secrets/problems.db.b64 > data/problems.db
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 8000
      - key: ALLOWED_ORIGINS
        value: "*"